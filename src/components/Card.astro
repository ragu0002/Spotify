---
const { header, img } = Astro.props;
---

<article>
  <img src={img} alt="bilde" />
  <h1>{header}</h1>
</article>

<style>
  article {
    display: flex;
    justify-content: start;
    background-color: rgba(143, 129, 129, 0.229);
    border-radius: 1rem;
    gap: 10px;
  }
  article:hover {
    opacity: 50%;
  }
  img {
    width: 15%;
  }
</style>
